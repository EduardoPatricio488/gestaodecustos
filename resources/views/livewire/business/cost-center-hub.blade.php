<div class="space-y-6 p-6"><div><h1 class="text-2xl font-semibold">Centros de custo</h1><p class="text-sm text-zinc-500">Organiza receitas e despesas por área, departamento ou projeto.</p></div><form wire:submit="save" class="grid gap-3 rounded-2xl border bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900 md:grid-cols-4"><input wire:model="code" placeholder="Código (ex.: ADM)" class="rounded-lg border p-2"><input wire:model="name" placeholder="Nome" class="rounded-lg border p-2"><input wire:model="description" placeholder="Descrição" class="rounded-lg border p-2 md:col-span-2"><button class="rounded-lg bg-zinc-900 px-4 py-2 text-white">Criar</button></form><div class="overflow-hidden rounded-2xl border"><table class="w-full text-sm"><thead><tr class="bg-zinc-50 text-left dark:bg-zinc-900"><th class="p-3">Código</th><th class="p-3">Nome</th><th class="p-3">Despesas</th><th class="p-3">Faturas</th><th class="p-3">Estado</th></tr></thead><tbody>@forelse($centers as $center)<tr class="border-t"><td class="p-3 font-medium">{{ $center->code }}</td><td class="p-3">{{ $center->name }}</td><td class="p-3">{{ $center->expenses_count }}</td><td class="p-3">{{ $center->invoices_count }}</td><td class="p-3"><button wire:click="toggle({{ $center->id }})" class="underline">{{ $center->is_active ? 'Ativo' : 'Inativo' }}</button></td></tr>@empty<tr><td colspan="5" class="p-6 text-center text-zinc-500">Ainda não existem centros de custo.</td></tr>@endforelse</tbody></table></div>@if($errors->any())<div class="rounded-lg bg-red-50 p-3 text-sm text-red-700">{{ $errors->first() }}</div>@endif</div>
